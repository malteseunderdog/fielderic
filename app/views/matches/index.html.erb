<h1>Listing matches</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Occurs</th>
    <th>Location</th>
    <th>Kind</th>
    <th>Players required</th>
    <th>Comment</th>
  </tr>

<% @match.each do |match| %>
  <tr>
    <td><%=h match.id %></td>
    <td><%=h match.occurs %></td>
    <td><%=h match.location %></td>
    <td><%=h match.kind %></td>
    <td><%=h match.players_required %></td>
    <td><%=h match.comment %></td>
    <td><%= link_to 'Show', match %></td>
    <td><%= link_to 'Edit', edit_match_path(match) %></td>
    <td><%= link_to 'Join', join_path(match) %></td>
    <td><%= link_to 'Destroy', match, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New match', new_match_path %>